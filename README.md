# Steam Purchase History Analyzer (анализатор истории покупок Steam)

Описание
-------
Небольшой Jupyter-ноутбук для анализа истории покупок в Steam с пересчетом сумм в рубли и корректировкой на инфляцию. Инструмент:

- Парсит CSV с историей покупок Steam
- Определяет типы транзакций (покупка, подарок, возврат, операции маркет-площадки и т.д.)
- Конвертирует суммы из валют (USD, EUR, GBP и др.) в рубли по историческим курсам ЦБ РФ
- Загружает данные об инфляции и пересчитывает цену каждой покупки в «современные» рубли
- Выводит удобный итоговый отчет: общие суммы, статистику по играм, подаркам, маркеты, внутриигровые покупки и возвраты

Файлы в репозитории
--------------------
- `Steam.ipynb` — основной ноутбук с кодом и инструкциями
- `steam_purchase_history.csv` — (пример) CSV с экспортированной историей покупок (не входит в репозиторий, добавляется пользователем)

Требования
----------
- Python 3.8+
- Рекомендуется использовать виртуальное окружение
- Библиотеки (установите через pip):

```bash
pandas
requests
pycbrf
beautifulsoup4
lxml
jupyter
```

## 📥 Как получить CSV-файл с историей покупок Steam

### ✅ Самый простой способ (рекомендуется):

Используйте расширение Chrome для автоматического экспорта истории:

1. Установите расширение для Chrome

    🔗 [Steam Purchase History Exporter (CSV)](https://chromewebstore.google.com/detail/bgbjngfnigoocbhciecckgfjbkkleghp)

2. 🌐 Откройте [Историю покупок Steam](https://store.steampowered.com/account/history/)

3. 📊 Нажмите кнопку **"Export"** в правом верхнем углу таблицы

4. ✨ CSV-файл автоматически загрузится в папку загрузок вашего браузера


### 📖 Чуть подробнее про расширение:
- **Автор:** [dogagcmnr на GitHub](https://github.com/dogagcmnr)
- **Репозиторий:** [steamexporter](https://github.com/dogagcmnr/steamexporter)
- **Что оно делает:** Парсит вашу историю покупок прямо из Steam и экспортирует в CSV

---
Как запустить
-------------
1. Активируйте виртуальное окружение (если используете):

```bash
source .venv/bin/activate  # macOS / Linux
# .\.venv\Scripts\activate  # Windows (PowerShell/Command Prompt)
```

2. Установите зависимости (если ещё не сделали):

```bash
pip install -r requirements.txt
```

3. Откройте `Steam.ipynb` в Jupyter Notebook / JupyterLab / VS Code и выполните ячейки сверху вниз.

Примечания по использованию
--------------------------
- Имя файла с историей по умолчанию: `steam_purchase_history.csv`. Если ваш файл называется иначе — измените переменную `steam_history` в первой части ноутбука.
- Если при загрузке CSV появляется ошибка — проверьте кодировку (UTF-8) и разделитель. Иногда дата/дескрипторы могут отличаться в разных локалях.
- Для корректной конвертации валют используется `pycbrf` (курсы ЦБ РФ). Убедитесь в наличии интернета при первом запуске.
- Данные об инфляции загружаются с публичного сайта; при проблемах с парсингом сайта проверьте соединение.

Структура выходных данных
-------------------------
После обработки ноутбук формирует DataFrame `df_parsed` с колонками:

- `date` — дата транзакции
- `type` — тип транзакции (Purchase, Gift Purchase, Market Transaction, Refund и т.д.)
- `games` — названия купленных игр/товаров (через запятую)
- `recipients` — получатели подарков (если применимо)
- `total` — сумма в рублях на дату покупки
- `total_inf` — сумма, пересчитанная с учётом инфляции до текущей даты

Результат и отчёт
-----------------
Запустите функцию `print_results(df_parsed)` в конце ноутбука — она выведет сводный отчёт с:

- общей суммой и количеством покупок
- суммой и списком подарков и получателей
- статистикой по операциям на маркете
- внутриигровыми покупками и возвратами

Кастомизация
------------
- Добавление новых символов валют: допишите их в словарь `CURRENCY_SYMBOLS` в ноутбуке.
- Исключение возвратов: воспользуйтесь секцией с `refunds_to_drop` (закомментирована — раскомментируйте при необходимости).
- Изменить поведение подсчёта: редактируйте функцию `print_results()`.

Технические замечания
--------------------
- Парсинг инфляции основан на HTML-таблице сайта [уровень-инфляции.рф](https://уровень-инфляции.рф/таблицы-инфляции); в случае изменения структуры сайта может потребоваться корректировка функции `get_inflation_data()`.

### 🎓 Если что то не работает или вы заметили ошибку, или в ващей истории покупок есть другие типы операций не учтенных в коде, или есть предложения по улучшению и доработке прошу связаться со мной.

[Telegram](@Maverick29rus)

[Email](Maverick29rus@gmail.com)

---

**Успехов в анализе! Если возникли вопросы - смотрите комментарии в коде 😉**

Контакты и ссылки
-----------------
- Репозиторий экспортера Steam: https://github.com/dogagcmnr/steamexporter

---

